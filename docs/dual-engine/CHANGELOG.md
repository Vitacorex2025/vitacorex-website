# Dual Engine Changelog

## 2026-03-21

### Layer 00

1. Created the `docs/dual-engine/` governance area.
2. Added the DocketMint Dual Engine Master Spec.
3. Added the master-pack execution order and copied Layers 00 and 01 into repo documentation.
4. Recorded the rule that the Python and Render shell is reference-only, not the long-term mainline.

### Layer 01

1. Added the hard product and architecture decisions document.
2. Added brand rules that ban Deadline CRM and separate generator positioning as active product naming.
3. Added product language rules for one-platform and two-engine messaging.
4. Logged repo-cutover and legacy naming risks for follow-up instead of hiding them.

### Layer 02

1. Added the Codex execution rules for narrow, dependency-aware layer passes.
2. Added the master-pack Layer 02 file so the execution order now includes implementation protocol guidance.
3. Added a pull request template that forces source-of-truth, scope, acceptance, and risk checks.
4. Added an execution changelog that records dependencies, outputs, and acceptance per completed layer.

### Layer 03

1. Added the main build sequence across six macro-phases: reset, foundation, engines, cross-linking, automation, and hardening.
2. Added milestone gates A through F with mandatory smoke-test expectations and gate-entry rules.
3. Added an ownership matrix across schema, frontend shell, legal engine, recovery engine, and docs and testing.
4. Recorded the rule that generation and packet work stays blocked until cross-linking is accepted.

### Layer 04

1. Added the stack decision that locks Next.js App Router, Vercel, and Supabase as the approved DocketMint foundation.
2. Added the environment strategy for local, preview, and production with one Next.js app and one Supabase project per environment.
3. Updated the execution board to mark Gate A as in progress now that the stack foundation is fixed.
4. Logged the current `vercel.json` as legacy bridge configuration rather than the target deploy story.
