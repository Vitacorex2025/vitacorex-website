V78 FULL ANALYSIS
1) Active pages load assets/css/styles.css and assets/js/site.js.
2) Prior breakage risk came from duplicate root files and asset files.
3) Service launcher is now injected into every real HTML page at the root of the site.
4) Launcher uses 4 buttons:
   - Home
   - Document / Individuals
   - Recovery / Pre-Collection
   - Gavel / Corporate Legal File Control
5) Buttons are green-tinted glass with layered gradients and 3D SVG artwork.
6) All old large quick-launch blocks and mobile dock blocks were removed.
7) Any TradingView fallback white widget behavior was neutralized:
   - no active TradingView container blocks remain in the HTML pages
   - legacy fallback function in JS now returns immediately
   - any .tradingview-widget-container classes are hidden in CSS
8) The market section remains local and should not render a white external widget window.
