VitaCoreX v23 final patch

Open index.html first.
Keep the full folder structure together, including assets/css, assets/js, assets/img, assets/video, and assets/pdf.
PDF files are stored in assets/pdf/.
Lead-gated PDF flow: user clicks a resource, completes Full Name + Company + Phone + Email, then the selected PDF opens.
Default site language is English. User language preference is saved in the browser.
The market ticker uses an external TradingView widget and needs internet access on the live site.
