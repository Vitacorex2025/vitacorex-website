# DocketMint Strict Template Library

This directory contains the controlling DOCX packet templates vendored from the uploaded files in this chat.

Mode: `strict_template_controlled`
Fallback templates: `disabled`

The backend will refuse packet generation if any of the required uploaded templates are missing.
